import pandas as pd


def send_email(
    results: pd.DataFrame,
    EMAILS: dict,
) -> None:
    
    pass


def send_whatsapp(
    results: pd.DataFrame,
    name: str,
    phone_to: str,
) -> None:
    
    pass